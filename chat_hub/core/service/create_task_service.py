from core.service import BaseService
# from infrastructure.rag.reflection 


class CreateTaskService(BaseService):
    def handle(self, session_id, query):
        # return chat(session_id, query)
        pass