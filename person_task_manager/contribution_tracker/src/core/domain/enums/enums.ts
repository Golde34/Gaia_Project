export enum DatabaseType {
    MYSQL = "mysql",
    MONGODB = "mongodb",
}

export enum CommitType {
    TASK = "task",
    SUBTASK = "subTask",
    GITHUB = "githubCommit",
    SCHEDULETASK = "scheduleTask",
}
